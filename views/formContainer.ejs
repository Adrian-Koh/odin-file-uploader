<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('navBar', links); %> <% if (formName === 'upload') { %> <%-
    include('fileUpload', folders); %> <% } else if (formName === 'login') { %>
    <%- include('login'); %> <% } else if (formName === 'signup') { %> <%-
    include('signup'); %> <% } else if (formName === 'folder') { %> <%-
    include('newFolder'); %> <% } %>
  </body>
</html>
